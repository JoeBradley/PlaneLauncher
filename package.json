{
  "name": "planelauncher",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "export NODE_ENV=production && node ./bin/www",
    "watch": "export USE_SSL=false && nodemon ./bin/www",
    "pc-start": "set USE_SSL=false && set USE_MOCK=true && node ./bin/www",
    "pc-watch": "set USE_SSL=false && set USE_MOCK=true && nodemon ./bin/www"
  },
  "dependencies": {
    "@assistant/conversation": "^3.2.0",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "docserver": "^0.1.4",
    "express": "~4.16.1",
    "morgan": "~1.9.1",
    "onoff": "^6.0.1",
    "pigpio": "^3.2.4",
    "pigpio-mock": "0.0.1",
    "socket.io": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
